const transferTx = await sdk.transferCheckedWithHook({
  source: sourceTokenAccount,
  mint: compliantMint,
  destination: destTokenAccount,
  owner: wallet,
  amount: 100,
  kycProof: [
    {
      provider: 'veriff',
      sessionId: 'session_123',
      timestamp: Date.now(),
      signature: '0x...'
    }
  ]
});
console.log('Transfer succeeded:', transferTx);